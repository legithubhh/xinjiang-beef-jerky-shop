# 云端更新指南

> 🚀 新疆手工牛肉干电商平台 - 云端服务器更新手册

**版本**: v1.1.0
**更新日期**: 2026-04-25
**仓库地址**: https://github.com/legithubhh/xinjiang-beef-jerky-shop

---

## 📋 更新流程概述

```
拉取代码 → 安装依赖 → 构建项目 → 重启服务
```

---

## 🔧 详细步骤

### 第一步：拉取最新代码

SSH 登录到云端服务器，进入项目目录执行：

```bash
# 进入项目目录
cd /path/to/xinjiang-beef-jerky-shop

# 拉取最新代码（确保在 main 分支）
git checkout main
git pull origin main
```

### 第二步：安装依赖

```bash
# 安装项目依赖
npm install
```

### 第三步：构建生产版本

```bash
# 构建前端资源
npm run build
```

### 第四步：重启服务

**方式 A：使用 PM2 进程管理器（推荐）**

```bash
# 重启 Node.js 服务
pm2 restart all

# 查看服务状态
pm2 status
```

**方式 B：使用 systemd 服务**

```bash
# 重启服务
sudo systemctl restart beef-jerky-shop

# 查看服务状态
sudo systemctl status beef-jerky-shop
```

**方式 C：手动重启**

```bash
# 停止现有服务（如果正在运行）
pkill -f "node server/index.cjs"

# 后台启动服务
nohup npm run server > /var/log/beef-jerky.log 2>&1 &

# 验证服务是否启动
curl http://localhost:3001/api/health
```

---

## ✅ 验证更新

### 检查服务状态

```bash
# 检查 API 服务
curl http://localhost:3001/api/health

# 预期返回：
# {"success":true,"message":"服务正常","timestamp":"..."}
```

### 检查前端资源

```bash
# 检查构建产物是否存在
ls -la dist/

# 检查图片资源
ls -la public/images/
```

### 访问网站

在浏览器中访问您的域名或 IP 地址，验证：
- 首页是否正常显示
- 美食滚动展示区是否正常滚动
- 各页面功能是否正常

---

## 🔄 自动化更新脚本（可选）

创建 `/opt/update-beef-jerky.sh` 脚本：

```bash
#!/bin/bash
# 新疆牛肉干网站 - 一键更新脚本

PROJECT_DIR="/path/to/xinjiang-beef-jerky-shop"
LOG_FILE="/var/log/beef-update.log"

echo "[$(date)] 开始更新..." | tee -a $LOG_FILE

cd $PROJECT_DIR

# 拉取代码
git pull origin main
if [ $? -ne 0 ]; then
    echo "[$(date)] Git pull 失败！" | tee -a $LOG_FILE
    exit 1
fi

# 安装依赖
npm install
if [ $? -ne 0 ]; then
    echo "[$(date)] npm install 失败！" | tee -a $LOG_FILE
    exit 1
fi

# 构建
npm run build
if [ $? -ne 0 ]; then
    echo "[$(date)] 构建失败！" | tee -a $LOG_FILE
    exit 1
fi

# 重启服务（根据实际情况选择）
pm2 restart all

echo "[$(date)] 更新完成！" | tee -a $LOG_FILE
```

设置执行权限：

```bash
chmod +x /opt/update-beef-jerky.sh
```

以后只需执行：

```bash
/opt/update-beef-jerky.sh
```

---

## 🆘 故障排除

### 问题 1：git pull 失败

**原因**：可能存在本地修改冲突

**解决**：
```bash
# 放弃本地修改，强制拉取
git fetch origin
git reset --hard origin/main
npm run build
```

### 问题 2：npm install 失败

**原因**：Node.js 版本不兼容或网络问题

**解决**：
```bash
# 检查 Node.js 版本
node -v

# 要求 Node.js >= 18.x
# 如果版本过低，请升级 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 问题 3：服务启动失败

**解决**：
```bash
# 查看错误日志
pm2 logs
# 或
cat /var/log/beef-jerky.log

# 常见问题：
# - 端口被占用：修改 .env 中的 PORT 或杀死占用进程
# - 数据库文件损坏：检查 data/database.sqlite 权限
```

### 问题 4：图片不显示

**解决**：
```bash
# 检查图片是否存在
ls -la public/images/

# 检查 public 目录结构
ls -la public/
```

---

## 📞 联系支持

- **项目负责人**: Jason
- **GitHub 仓库**: https://github.com/legithubhh/xinjiang-beef-jerky-shop

---

## 📝 更新日志

| 日期 | 版本 | 内容 |
|------|------|------|
| 2026-04-25 | v1.1.0 | 新增首页美食滚动展示区（顺序/倒序播放） |
| 2026-03-22 | v1.0.0 | 项目初始化，基础功能上线 |

---

> 📌 **提示**：建议在业务低峰期进行更新，避免影响用户体验。
